<custom-picker range="{{ range }}" active="{{ active }}" bind:columnchange="onColumnChange" bind:change="onChange" bind:cancel="onCancel">
    <view bindtap="btntap">自己写的picker</view>
</custom-picker>